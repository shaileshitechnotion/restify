#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../restify-bunyan-logger/bin/restify-bunyan-logger" "$@"
  ret=$?
else 
  node  "$basedir/../restify-bunyan-logger/bin/restify-bunyan-logger" "$@"
  ret=$?
fi
exit $ret
